errlogInit(20000)

dbLoadDatabase("{{ deploy_ioc_required_module_path }}/dbd/{{ deploy_ioc_executable }}.dbd")
{{ deploy_ioc_executable }}_registerRecordDeviceDriver(pdbbase)

ADUVCConfig("$(PORT)", "$(SERIAL_NUM_OR_PRODUCT_ID)")

# Wait two seconds for ensuring camera initialized correctly
epicsThreadSleep(2)

asynSetTraceIOMask($(PORT), 0, 2)

# Main database
dbLoadRecords("$(ADUVC)/db/ADUVC.template", "P=$(PREFIX),R=cam1:,PORT=$(PORT)")
